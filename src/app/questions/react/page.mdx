import { Questions } from '@/components/questions';
import GithubEdit from '@/components/github-edit';

export const metadata = {
  title: 'React Questions',
  description: 'React interview questions with answers',
  alternates: {
    canonical: '/questions/react/',
  },
};

<Questions category='react' />

<GithubEdit link='/questions/react/page.mdx' />
